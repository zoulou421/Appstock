
{% extends 'template.html.twig' %}

{% block title %}
    
{% endblock %}

{% block body %}

   {#{ form(formpersonne) }#} 
   {{ form_start(formpersonne, {'attr': {'novalidate': 'novalidate'}}) }}
   <div class="row">
        <div class="col">{{ form_row(formpersonne.firstname) }}</div> 
        <div class="col">{{ form_row(formpersonne.name) }}</div> 
        
        {{ form_rest(formpersonne) }}
   </div>
   
    
{% endblock %}

{% block javascripts %}
  <script>
    $(document).ready(function() {
    $('.select2').select2();
    });
  </script>
{% endblock %}